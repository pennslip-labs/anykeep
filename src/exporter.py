class Exporter:
    """Placeholder exporter for future Anytype bundle generation."""

    def __init__(self, output_path=None):
        self.output_path = output_path

    def export(self, objects):
        return list(objects)
